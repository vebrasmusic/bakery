interface:
  display_name: "Bakery Worktree Bootstrap"
  short_description: "Patch Node repos with bootstrap-bakery.sh for deterministic Bakery ports into .env.bakery"
  default_prompt: "Patch this Node repo with a bootstrap-bakery.sh that allocates slice ports and writes deterministic Bakery keys to .env.bakery."
