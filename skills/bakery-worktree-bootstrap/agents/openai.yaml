interface:
  display_name: "Bakery Worktree Bootstrap"
  short_description: "Patch Node repos for deterministic Bakery setup.sh env scaffolding"
  default_prompt: "Patch this Node repo with a minimal Bakery setup.sh that allocates slice ports and writes deterministic .env keys."
